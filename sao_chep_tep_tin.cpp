#include <bits/stdc++.h>
using namespace std;
int main()
{
    // ifstream fp;
    // fp.open("PTIT.in");
    // ofstream fp1;
    // fp1.open("PTIT.out");
    // string a;
    // while (getline(fp, a))
    //     fp1 << a << endl;
    // fp.close();
    // fp1.close();
    // return 0;

    freopen("PTIT.in", "r", stdin);
    freopen("PTIT.out", "w", stdout);
    string a;
    while (getline(cin, a))
        cout << a << endl;
    return 0;
}